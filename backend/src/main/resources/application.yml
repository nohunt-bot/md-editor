server:
  port: 8080

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/skillmd

app:
  audit:
    retention-days: 90
  version:
    retention-count: 50

logging:
  level:
    com.company.skillmd: DEBUG
