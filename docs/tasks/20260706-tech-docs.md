# Task: 補齊技術文件（啟動設定 / 登入手順 / 使用者操作手順）

Status: done (2026-07-06 — sonnet 實作、commander 驗收通過、merge，已推 GitHub)

> 派 sonnet，**硬隔離 worktree**：/Users/ch/md-editor-wt/docs-tech（branch
> wt/docs-tech，base 79d789c）。commander 驗收（對照實碼準確度 + 完整度 +
> main tree 保持乾淨），過關 merge。

## Acceptance criteria
- [ ] docs/guide/ 下三份文件 + 索引：setup（啟動服務）、login（登入/身分）、
      user-flows（使用者操作手順），每步驟具體可照做
- [ ] 內容對照實碼正確：身分是 dev-stub（X-Dev-User），非 Keycloak；啟動用
      本機直跑（compose build 有憑證問題）；port/seed/指令正確
- [ ] 涵蓋所有現有功能流程（見下方清單），無遺漏、無杜撰
- [ ] main tree 保持乾淨（agent 只動 worktree）；純文件、不動程式碼

## 必涵蓋清單（交派工）
啟動：前置需求、mongo、backend、frontend、seed、port、env、compose 憑證繞過。
登入：dev-stub 模型、X-Dev-User、四個 dev 使用者、切換身分、Keycloak deferred。
操作：建立→發布、下架、複製到團隊（成員感知）、搜尋/分頁/tag、版本歷史+還原、
讚+引用、資料夾（modal）、設定（主題/語言持久化）、深色、多語系、在線提示。

## Progress log
- 2026-07-06 | planning | 硬隔離 worktree 自建；派 sonnet
- 2026-07-06 | done | sonnet commit 9a42cb0（worktree wt/docs-tech）。
  ★★ 硬隔離成功——main tree 全程乾淨、docs/guide 不在 main、work 在分支上
  （對比上一個任務 agent 直接落 main）。commander 驗收：讀三份文件逐項對照
  實碼——dev-users/health/DevCurrentUserProvider/application.yml 皆一致、
  10 flow 完整、缺口如實標註（references API-only、compose 憑證、Keycloak
  deferred、editor folder 下拉未載入）。merge 6a83a7f。★ 新流程驗證有效：
  commander 自建 worktree + 絕對路徑派工 = 真隔離。
